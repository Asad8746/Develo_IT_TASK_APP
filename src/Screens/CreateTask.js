import React from "react";
import { View, StyleSheet, Text } from "react-native";
import { Container } from "../Components";
export const CreateTaskScreen = () => {
  return (
    <Container>
      <Text>Hello Create Screen</Text>
    </Container>
  );
};

const styles = StyleSheet.create({});
