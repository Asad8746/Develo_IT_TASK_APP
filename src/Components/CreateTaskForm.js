import React from "react";
import { View, StyleSheet, Text } from "react-native";

export const CreateTaskForm = () => {
  return (
    <View>
      <Text>Create Task Form</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
